# ansible-galaxy install -r requirements.yml
# ansible-playbook playbook.yml -i inventory.ini -kK



- hosts: all
  become: yes
 
  tasks:
    - name: install pip
      ansible.builtin.apt:
          name: python3-pip=23.0.1+dfsg-1 
          state: present
          update_cache: yes
      tags: 
      - prep

  roles:
    - role: geerlingguy.pip
      tags: 
      - prep         